{
  "name": "glycemic",
  "version": "1.0.0",
  "description": "",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc && npm run build:client",
    "build:client": "tsc -p tsconfig.client.json && mv public/client/script.js public/script.js && rm -rf public/client public/shared",
    "prestart": "npm run build",
    "start": "node dist/src/index.js",
    "dev": "tsc --watch",
    "test": "npm run build && node --test dist/src/**/*.test.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.10.1",
    "multer": "^2.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/morgan": "^1.9.10",
    "@types/multer": "^2.0.0",
    "@types/node": "^24.0.14",
    "typescript": "^5.8.3"
  }
}
